import React, {useState, useEffect} from 'react';
import { TouchableOpacity, Text, View, Button, Image} from 'react-native';
import styles from '../styles/HomeStyle';

var WifiManager = require('react-native-wifi-manager');

export default function wifi_set_up() {
    var wifiArray = ["Hurtakis"]
    return (
        <View>
            <Text style={styles.title}> Wifi Setup Happens Here, Not yet implemented </Text>
        </View>
    );
}  




